source "https://rubygems.org"

# Remove the explicit "jekyll" gem. 
# The "github-pages" gem will automatically install the correct version (Jekyll 3.9.x)
gem "github-pages", group: :jekyll_plugins

# Required for Ruby 3.0+ to run locally
gem "webrick"
